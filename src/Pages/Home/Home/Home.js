import React from 'react';
import Input from './Input';
import Counter from '../Counter/Counter';

const Home = () => {
    return (
        <div>
            <Input/>
            <Counter/>
        </div>
    );
};

export default Home;